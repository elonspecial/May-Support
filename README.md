# Maybank-inspired login prototype

A local, front-end-only login screen inspired by Maybank's yellow and black visual language.

This is a design prototype. The form does not submit or transmit credentials.

## Run locally

Open `index.html` in a browser, or run:

```powershell
python -m http.server 4173
```

Then visit http://localhost:4173.
